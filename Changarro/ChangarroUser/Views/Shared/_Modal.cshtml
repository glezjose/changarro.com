<div id="modal-render" class="modal-dialog" role="document">
    <div class="modal-content login_form_inner_modal">
        @RenderSection("header", required: false)
        <div class="modal-body">
            @RenderBody()
        </div>
        @RenderSection("footer", required: false)
    </div>
</div>
